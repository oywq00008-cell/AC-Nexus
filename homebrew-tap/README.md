# BroadlinkAC Homebrew Tap

macOS 一键安装 BroadlinkAC：

```bash
brew install --cask oywq00008-cell/broadlinkac/broadlinkac
```

首次运行前清除隔离标记（Homebrew 会自动执行，如未生效可手动）：

```bash
xattr -cr /Applications/BroadlinkAC.app
```
